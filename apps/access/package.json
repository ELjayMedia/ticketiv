{
  "name": "@ticketiv/app-access",
  "version": "0.0.0",
  "private": true,
  "description": "Ticketiv Access staff app (com.ticketiv.access) — React Native app core for scanner workflows.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "test": "vitest run src/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ticketiv/adapters": "workspace:*",
    "@ticketiv/shared": "workspace:*",
    "@ticketiv/tokens": "workspace:*"
  }
}
