export {
  normalizeCredentialPublicId,
  scannerResultFromPayload,
  scannerStatusTitle,
  scannerStatusTone,
  shouldSuppressTapBandRead,
} from "@ticketiv/shared"

export type {
  ScannerClientResult,
  ScannerClientStatus,
  ScannerInputMode,
  TapBandCooldownState,
} from "@ticketiv/shared"
